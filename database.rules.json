{
  "rules": {
    ".read": true,
    "comments": {
      "$object": {
      	".validate": "newData.hasChild('comment') && newData.child('comment').val().length < 140"
      }
    },
    ".write": "auth != null"
  }
}